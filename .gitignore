
# Beads / Dolt files (added by bd init)
.dolt/
*.db
.beads-credential-key
.beads/proxieddb/
# Sidecar capture output (runtime data, not source)
sidecar/capture.jsonl
sidecar/sidecar.log
# Python bytecode (runtime, not source)
__pycache__/
